function palindrome (str)
{
	var palindrome1 = "";
	for (var i=str.length -1; i >= 0; i--)
	{
	  	palindrome1 += str[i];
	  //console.log(palindrome1);
	}

	if (palindrome1 === str)
	{
		return true
	}
	else return false;
}

palindrome ("racecar");
